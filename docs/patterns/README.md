# デザインパターン編（全23章）

この講習は、**結城浩『増補改訂版 Java言語で学ぶデザインパターン入門』の輪読と並走する**ためのものです。
本の内容は繰り返しません。**本を手元に開いた状態で読んでください。**

扱うのは 1 点だけです。

> **その章のパターンを C++ で書くと、Java 版と何が変わるのか。**

## 他のトラックとは独立しています

この講習は **C++入門編・C++編・ROS 2編とは別のトラック**です。
それらを読んでいなくても、上から順に読めます。逆に、この講習を読まなくても
他のトラックは完結します。**順番の依存はありません。**

主題は「部活で自分たちのライブラリを設計するとき」であって、
rclcpp を読むことではありません。rclcpp の話は各章の末尾に**補足として**置くだけです。

## なぜ別の講習が要るのか

結城本は Java で書かれています。Java と C++ は、パターンの実装に直接効く前提が違います。

| 結城本の前提 | C++ ではどうなるか |
| --- | --- |
| GC がある。オブジェクトは勝手に消える | **誰が解放するかを型で表明する**必要がある。`unique_ptr` / `shared_ptr` / 参照 / 値 |
| すべての変数が参照（プリミティブ以外） | 値セマンティクスがある。何も考えずに書くと**コピーが走る** |
| `interface` はいくらでも実装できる | 純粋仮想クラスの多重継承。菱形継承と `virtual` 継承の問題が出る |
| `null` を返せる | `nullptr` を返すか `std::optional` を返すかを選ぶ |
| `clone()` / `Cloneable` | コピーコンストラクタが既にある。`clone()` は**多態のときだけ**必要 |
| `finalize()` は使うなと言われる | デストラクタと RAII が主役。むしろ C++ の方が強い |
| すべてのメソッドが仮想 | `virtual` を書かないと仮想にならない。**書き忘れが即バグ** |

この差を埋めないまま Java 版を写経すると、**動くけれど寿命が壊れているコード**ができます。
部活のライブラリで一番起きてほしくない事故がこれです。

## この講習の使い方

各記事は次の形で固定しています。

```
> 結城本 第N章 対応

1. Java 版の構造をそのまま C++ にすると何が起きるか
2. C++ ではどう書くのが素直か
3. 実装する（→ 課題へ）
4. 標準ライブラリに同じものが無いか
5. マイコンでの結論
6. ROS 2 での結論
```

**「実装する」は必ず通します。** どのパターンも一度は手で書きます。
そのうえで「標準ライブラリに同じものが無いか」を毎回確認します。
C++ では GoF のいくつかが**既に言語機能や標準ライブラリになっている**ので、
自作したものと標準のものを並べて初めて「なぜ標準はこの形なのか」が分かります。
自作を飛ばすとその比較ができません。

## 章の対応表

「C++ での書き換え」欄は、**実装しなくていいという意味ではありません。**
実装したうえで、実務で採用するときにどう形を変えるか、です。

### 第1部 デザインパターンに慣れる

| 章 | パターン | C++ での書き換え | 課題 |
| --- | --- | --- | --- |
| [1](01_Iterator.md) | Iterator | GoF 版を書いたあと、**STL イテレータの作法**（`begin()` / `end()`）に寄せる | `dp01` |
| 2 | Adapter | 多重継承版は使わず**委譲版**一択 | `dp02` |

### 第2部 サブクラスにまかせる

| 章 | パターン | C++ での書き換え | 課題 |
| --- | --- | --- | --- |
| 3 | Template Method | **NVI**（public 非仮想 → private 仮想）。仮想デストラクタ必須 | `dp03` |
| 4 | Factory Method | 生ポインタではなく `std::unique_ptr<Product>` を返す | `dp04` |

### 第3部 インスタンスを作る

| 章 | パターン | C++ での書き換え | 課題 |
| --- | --- | --- | --- |
| 5 | Singleton | Java の static フィールドは**初期化順序で壊れる**。Meyers Singleton へ | `dp05` |
| 6 | Prototype | `clone()` は共変戻り値 + `unique_ptr`。コピーコンストラクタとの役割分担 | `dp06` |
| 7 | Builder | メソッドチェーンと**参照修飾**（`&&` オーバーロード）まで | `dp07` |
| 8 | Abstract Factory | ほぼそのまま。ただし**過剰適用の筆頭** | `dp08` |

### 第4部 分けて考える

| 章 | パターン | C++ での書き換え | 課題 |
| --- | --- | --- | --- |
| 9 | Bridge | **Pimpl と同族**。ABI とコンパイル時間の話が絡む | `dp09` |
| 10 | Strategy | 仮想関数 / `std::function` / テンプレートの**3択**。マイコンで選択が変わる | `dp10` |

### 第5部 同一視

| 章 | パターン | C++ での書き換え | 課題 |
| --- | --- | --- | --- |
| 11 | Composite | 構造は同じ。難所は**子の所有権**（`unique_ptr` の vector） | `dp11` |
| 12 | Decorator | 入れ子の `unique_ptr`。ムーブで組み立てる | `dp12` |

### 第6部 構造を渡り歩く

| 章 | パターン | C++ での書き換え | 課題 |
| --- | --- | --- | --- |
| 13 | Visitor | GoF 版の二重ディスパッチを書いたあと、**`std::variant` + `std::visit`** と比較 | `dp13` |
| 14 | Chain of Responsibility | 構造は同じ。連鎖の**寿命**が難所 | `dp14` |

### 第7部 シンプルにする

| 章 | パターン | C++ での書き換え | 課題 |
| --- | --- | --- | --- |
| 15 | Facade | クラスにする必要は薄い。**名前空間 + 自由関数**で足りる場合が多い | `dp15` |
| 16 | Mediator | `shared_ptr` の相互参照で**循環**する。`weak_ptr` が要る | `dp16` |

### 第8部 状態を管理する

| 章 | パターン | C++ での書き換え | 課題 |
| --- | --- | --- | --- |
| 17 | Observer | **この講習の山場。** 購読者が先に死ぬ問題。`weak_ptr` と購読解除 | `dp17` |
| 18 | Memento | 値セマンティクスがあるので Java より簡単になる | `dp18` |
| 19 | State | クラス版を書いたあと、`enum` + `switch` / `std::variant` と比較 | `dp19` |

### 第9部 無駄をなくす

| 章 | パターン | C++ での書き換え | 課題 |
| --- | --- | --- | --- |
| 20 | Flyweight | `shared_ptr` による共有と `std::string_view` | `dp20` |
| 21 | Proxy | **スマートポインタそのもの**が Proxy。`operator->` を書く | `dp21` |

### 第10部 クラスで表現する

| 章 | パターン | C++ での書き換え | 課題 |
| --- | --- | --- | --- |
| 22 | Command | `std::function` とコマンドキュー。マイコンでは確保が問題になる | `dp22` |
| 23 | Interpreter | ほぼそのまま。C++ 固有の話は少ない | `dp23` |

## マイコンと ROS 2 で結論が割れます

部活では**マイコンと ROS 2 の両方**を書きます。この 2 つは前提が違うので、
各記事の末尾に必ず両方の結論を置きます。先に差分をまとめておきます。

| 論点 | マイコン | ROS 2（Linux 上） |
| --- | --- | --- |
| 動的確保（`new` / `make_unique`） | **原則、起動時のみ**。ループ中は禁止 | 自由。ただしリアルタイム経路は避ける |
| 例外 | `-fno-exceptions` が普通。`throw` できない | 使える |
| RTTI / `dynamic_cast` | `-fno-rtti` で**使えないことが多い** | 使える |
| 仮想関数 | 使えるが vtable の ROM/RAM コストがある。乱発しない | 気にしなくてよい |
| `std::function` | **ヒープ確保しうる**。関数ポインタかテンプレートに置換 | 使ってよい |
| `std::string` | 確保が走る。固定長バッファか `string_view` | 使ってよい |
| Singleton | ペリフェラルは実質シングルトン。**初期化順序が本番** | 避けられる。避けるべき |
| Strategy の実装手段 | テンプレート（コンパイル時）を第一候補に | 仮想関数 / `std::function` でよい |

この表があるので、**同じパターンでも「マイコンではこう書く」「ROS 2 ではこう書く」が違って当然**です。
記事の中で毎回はっきり分けます。

## 課題の動かし方

課題は ROS 2 編・C++ 編と同じ `drill` で動きます。**ROS 2 は使いません。**
素の C++17 と gtest だけです。

```bash
./drill list              # 一覧
./drill run dp01          # 第1章の課題
./drill watch dp01        # 保存するたびにテスト
./drill hint dp01         # ヒント
./drill solution dp01     # 解答例
```

マイコン向けの制約（`-fno-exceptions` など）は**課題のビルド設定には入れていません**。
ビルド環境で詰まる時間を減らすためです。制約下でどう書くかは、各記事の
「マイコンでの結論」にコードで載せます。

## 前提

**必須**

- 結城浩『増補改訂版 Java言語で学ぶデザインパターン入門』（SBクリエイティブ）
- C++ で継承・仮想関数・`std::unique_ptr` / `std::shared_ptr` を書いたことがある

**詰まったときの参照先**（読んでいる必要はありません）

| 詰まったところ | 参照 |
| --- | --- |
| 参照と値の違い | [参照](../cpp-basics/03_参照.md)・[値のセマンティクス](../cpp-basics/09_値のセマンティクス.md) |
| `virtual` / `override` / 仮想デストラクタ | [継承](../cpp/03_継承.md) |
| `unique_ptr` / `shared_ptr` / `weak_ptr` | [スマートポインタ](../cpp/06_スマートポインタ.md) |
| ムーブ | [ムーブと所有権](../cpp/05_ムーブと所有権.md) |
| ラムダと `std::function` | [ラムダと `std::bind`](../cpp/07_ラムダとstd_bind.md) |

**所有権の話が 23 章のうち 15 章くらいで本題になります。**
`unique_ptr` があやしいと思ったら、先に上の表の該当章を見てください。

## 進めかた

**輪読の進度を追い越さないでください。** 本を読んでいない章の記事を先に読んでも、
「本と何が違うか」という軸が効きません。1 章読む → この講習の同じ章を読む → 課題、の順です。

各章 45〜90 分。23 章で 25〜35 時間が目安です。

## 最初に読むもの

パターンを覚えた直後がいちばんコードを壊します。
[0. 使う前に](00_使う前に.md) を先に読んでください。5 分です。
