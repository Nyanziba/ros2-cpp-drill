// このファイルは編集しません（インタフェースの提示）。
#pragma once

// 呼び出し元の 2 つの int を入れ替えます。
void swap_values(int & a, int & b);

// 大きい方への参照を返します。
// a == b のときは a への参照を返してください（テストがそこを見ています）。
int & largest(int & a, int & b);
